const data={work:{'running late':['Running late, joining soon','Running behind, will join shortly','Running late, almost there']}};function selectCategory(cat){let html='';Object.keys(data[cat]).forEach(s=>{html+=`<button onclick="showReplies('${cat}','${s}')">${s}</button>`});document.getElementById('situations').innerHTML=html;}function showReplies(cat,sit){let list=data[cat][sit];let html=list.map(r=>`<div onclick="copy('${r}')">${r}</div>`).join('');document.getElementById('replies').innerHTML=html;}function copy(text){navigator.clipboard.writeText(text);alert('Copied');}function saveEmail(){alert('Saved');}