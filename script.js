document.addEventListener("DOMContentLoaded",()=>{
const data={work:["Running late","Need more time"]};
const cat=document.getElementById("categories");
if(cat){
cat.innerHTML='<button>Work</button>';
}
});